pipeline{
    agent any
    stages {
        stage ('Checkout Source using Tag') {
            steps {
                sh 'rm -rf'
                sh 'git clone -b "@deploy1ImageVersion" --branch "master" "https://github.com/Thuji98/patient-management.git" && cd "patient-management"'

            }
        }

    }
}