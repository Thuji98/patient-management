pipeline{
    agent any
    stages {
        stage ('Checkout Source using Tag') {
            steps {
                sh 'git clone -b "@deployImageVersion" --branch "master" "https://github.com/Thuji98/patient-management.git"'
            }
        }

    }
}