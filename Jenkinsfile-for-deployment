pipeline{
    agent any
    stages {
        stage ('Checkout Source using Tag') {
            steps {
                sh 'git clone --depth 1 --branch "deployImageVersion" "https://github.com/Thuji98/patient-management.git"'
            }
        }

    }
}